# Intentionally empty. The exporter's API key comes from the mounted
# sabnzbd_api_key secret via the entrypoint shim in docker-compose-observability.yml,
# and SABNZBD_BASEURLS is set inline under `environment:`, so no variables
# belong here. The file must exist only because compose's env_file: fails
# start if the path is missing.
