# Copy this file to .env.secrets and fill in real values.
# .env.secrets is gitignored and overrides configs/calibre/.env.
# Empty: the desktop GUI's CUSTOM_USER/PASSWORD are supplied as compose
# secrets and read via FILE__CUSTOM_USER/FILE__PASSWORD instead. See
# configs/calibre/secrets/ and docs/COMPOSE_CONVENTIONS.md.
