# Credentials for the external storage mount, consumed by mount.cifs via
# STORAGE_CREDENTIALS_FILE in .env. Copy to .smbcredentials and fill in.
# Keep it mode 0600: mount.cifs is run with sudo and this file holds a
# password in plain text.
username=
password=
domain=WORKGROUP
